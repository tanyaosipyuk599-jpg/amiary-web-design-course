# AMIARY landing-page prototype

A responsive static landing-page prototype for AMIARY, a private memory portrait made as a gift.

Open `index.html` in a browser to view the site locally. The page uses relative paths to the images in `assets/images/`.
